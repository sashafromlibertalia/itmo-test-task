import React from 'react';

import HeaderContainer from "./layout/HeaderContainer";

const Header = () => {
    return (
        <HeaderContainer>
            Тут заголовок
        </HeaderContainer>
    );
};

export default Header;