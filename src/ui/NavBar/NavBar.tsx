import React from 'react';

import NavBarContainer from "./layout/NavBarContainer";

const NavBar = () => {
    return (
        <NavBarContainer>
        </NavBarContainer>
    );
};

export default NavBar;